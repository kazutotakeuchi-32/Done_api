<p> <%= @resource.email %>様　</p>

<p><%= t(".instruction")%></p>

<p><%= link_to t(".action"), edit_password_url(@resource, reset_password_token: @token,config: message['client-config'].to_s, redirect_url: message['redirect-url'].to_s) %></p>
